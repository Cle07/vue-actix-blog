#[cfg(test)]
mod test {
    #[test]
    fn db_initialization_test() {
        // Should check both the case where .md are present in /articles, and case where no .md are present, then the warning should be printed.
    }
}
